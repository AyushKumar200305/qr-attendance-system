STEP 1 — Security hardening

Replace hardcoded app.secret_key with os.environ.get('SECRET_KEY', 'dev-key')
Add python-dotenv to requirements, load .env at startup
Add ANTHROPIC_API_KEY = os.environ.get('ANTHROPIC_API_KEY', '') at top of app.py
Add rate limiting: track failed PIN attempts in Flask session, lock teacher login for 5 minutes after 5 wrong PINs, show countdown timer on the lockout screen


STEP 2 — AI Student Chatbot

Add route POST /api/ai/student-chat — takes roll, message, history — fetches real student data using existing get_student_stats and predict_detention_risk, sends to Claude API model claude-sonnet-4-20250514 max_tokens 400 as an academic advisor with full attendance context
In student_stats.html add a floating chat bubble button bottom-right corner that opens a full chat panel. Include quick-ask buttons: "Am I at risk?", "Worst subject?", "How many classes can I skip?", "Will I get detained?"
Chat history persists during the page session using a JS array


STEP 3 — AI Teacher Intelligence Report

Add route POST /api/ai/teacher-report (teacher login required) — reads get_class_insights() and get_all_students_risk(), sends to Claude, gets a structured report with sections: Overall Health, Students Needing Urgent Attention, Trend Analysis, Recommended Actions
In teacher_dashboard.html add a card with a "Generate AI Report" button. Display the report with each section styled differently. Add a "Copy Report" button that copies text to clipboard


STEP 4 — AI Anomaly Explainer

Add route POST /api/ai/explain-anomaly (teacher login required) — takes type, description, severity, sends to Claude, returns EXPLANATION (2 sentences plain English) and ACTION (one specific step)
In anomalies.html add an "Explain" button on every row in both the flagged table and anomaly log table. Show the AI response inline below the row, styled as a bordered box


STEP 5 — PDF Report Generation

Add reportlab>=4.0.0 and matplotlib>=3.7.0 to requirements.txt
Add route GET /student/<roll>/download-report:

Fetch student data using existing functions
Generate a matplotlib bar chart: one bar per subject, colored green ≥75%, orange 60–74%, red <60%, dotted green line at 75%, value labels on each bar
Call Claude API for a 3–4 sentence personalized risk analysis
Build PDF with ReportLab: styled header, student info table (name, roll, overall %, risk status, date), bar chart, full subject breakdown table (subject name, attended, total, %, status, risk level), AI analysis in a styled callout box, footer with generation timestamp
Return as download with filename attendance_<roll>.pdf


In student_stats.html add a "Download PDF Report" button near the top


STEP 6 — Live Dashboard with Real-time Updates

In teacher_dashboard.html wrap attendance counts for active sessions in <span data-session="{{s.id}}">{{s.att_count}}</span>
Add JavaScript that polls /api/session/<id>/count every 8 seconds for all active sessions and updates counts live with a brief green flash animation when count changes
Add a live "Last updated" timestamp that updates every second


STEP 7 — Advanced Analytics Dashboard

Add route GET /teacher/analytics (teacher login required) that returns a full analytics page
In database.py add a function get_advanced_analytics() that returns: per-subject attendance trend for last 30 days, top 5 most absent students, busiest attendance day of week, average session size, total scans blocked by anomaly detection
In the analytics page render: a line chart using Chart.js CDN showing 30-day attendance trend per subject (one colored line per subject), a bar chart showing attendance by day of week, a table of top absent students with their percentages and risk badges, summary stat cards (total students, total sessions, total attendance marks, total anomalies blocked)
Add a link to this page in the teacher nav and on teacher_dashboard.html


STEP 8 — Smart Student Notifications

In student_stats.html at the very top of the content block, add smart alert banners based on actual records data:

If any subject below 60%: red banner listing those subjects with exact percentage
If any subject between 60–75%: orange banner listing those subjects
If all subjects above 75%: one green banner saying all clear
Each banner is dismissible with an X button




STEP 9 — Live Search and Filter on Students Page

In all_students.html add a search bar above the table that filters rows live as you type, matching against roll number or name (case-insensitive, pure JavaScript)
Add filter buttons: All / Critical / Danger / Warning / Safe — clicking highlights the button and hides non-matching rows
Show a live count like "Showing 12 of 45 students" that updates as filters change


STEP 10 — Bulk Operations for Teacher

In all_students.html add checkboxes on each student row and a "Select All" checkbox in the header
Add a bulk action bar that appears when any checkbox is checked, with options: "Export Selected CSV" and "Delete Selected" with a confirm dialog showing count of selected students
Add route POST /teacher/students/bulk-delete that accepts a JSON list of student IDs and deletes them with all their attendance records
Add route POST /teacher/students/bulk-export that returns a CSV of only the selected students


STEP 11 — Subject Management

In database.py add functions: add_subject(code, name), delete_subject(code), get_all_subjects() — store custom subjects in a new subjects table, fall back to hardcoded SUBJECTS dict if table is empty
Update all references to SUBJECTS in app.py, database.py, ml_engine.py to use get_all_subjects() instead of the hardcoded dict
In teacher_dashboard.html add a subject management card: input fields for code and name with an Add button, and a list of current subjects each with a Delete button. Show a warning if trying to delete a subject that has attendance records


STEP 12 — Attendance Correction System

Add route POST /teacher/attendance/remove (teacher login required) that takes student_id and session_id and removes a specific attendance record
Add route POST /teacher/attendance/add-manual that allows teacher to manually add attendance for a student to a specific session
In session_detail.html add a Remove button next to each attendee row, and an "Add Student Manually" form at the bottom with roll number input


STEP 13 — QR Session Scheduling

Add optional fields to the generate QR form in generate_qr.html: Max Students (number input, default unlimited), Notes (text field for teacher notes about the session)
Save these to the qr_sessions table — add columns max_scans (already exists) and notes TEXT DEFAULT ''
When a student tries to mark attendance and the session is at max capacity, show a friendly error page saying the session is full
Show notes in the session detail page


STEP 14 — Update requirements.txt
Replace the full contents of requirements.txt with:
flask>=3.0.0
qrcode[pil]>=7.4.2
Pillow>=10.0.0
python-dotenv>=1.0.0
anthropic>=0.25.0
reportlab>=4.0.0
matplotlib>=3.7.0
gunicorn>=21.0.0

STEP 15 — Final setup

Run pip install -r requirements.txt
Add ANTHROPIC_API_KEY and SECRET_KEY to Replit Secrets panel
Run python app.py
Do not break any existing routes or functionality
Keep the existing dark theme throughout all new pages and components
All new AI routes use claude-sonnet-4-20250514 via the Anthropic Python SDK
All new pages extend base.html


That's it. Paste this entire prompt into Replit Agent and it will build everything automatically.You are out of free messages until 11:20 PMKeep workingSonnet 4.6